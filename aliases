# Unix
alias tlf="tail -f"
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='../..'
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'
alias -g G='| grep'
alias -g M='| less'
alias -g L='| wc -l'
alias -g ONE="| awk '{ print \$1}'"
alias e="$EDITOR"
alias v="$VISUAL"

# git
alias gci="git pull --rebase && rake && git push"
alias stpp=subtree push --prefix

# project aliases
alias dotfiles="cd ~/.dotfiles && sublime ."

# home folder
alias home="cd ~"

# NPB
alias sshnpb="ssh deploy@192.184.83.75"
alias npb="cd ~/Projects/npb/"
alias npbdeploy="git push github master && cap production deploy"

# SCAREY
alias scarey="cd ~/Projects/scarey.org/site"
alias sshscarey="ssh -p 2222 nick@gator1332.hostgator.com"

# LURAY
alias luray="cd ~/Projects/luraymusic/site"
alias sshluray="ssh -p 2222 luray@gator3036.hostgator.com"

# VANILLA SLAB
alias vslab="cd ~/Projects/vanilla-slab"

# SITE
alias projects="cd ~/Projects"

# the air on earth
alias air="cd ~/Projects/theaironearth"


# LOUDR
alias loudrw='cd ~/Loudr/loudr-web && workon loudrweb && sublime --project LoudrJS.sublime-project'
alias loudrwiki='cd ~/Loudr/loudr-web.wiki'
alias loudrwebserv='./start.py'
alias workoff='deactivate'

