# Unix
alias tlf="tail -f"
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='../..'
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'
alias -g G='| grep'
alias -g M='| less'
alias -g L='| wc -l'
alias -g ONE="| awk '{ print \$1}'"
alias e="$EDITOR"
alias v="$VISUAL"

# git
alias gci="git pull --rebase && rake && git push"
alias stpp=subtree push --prefix

# Bundler
alias b="bundle"

# Tests and Specs
alias t="ruby -I test"

# Rails
alias migrate="rake db:migrate db:rollback && rake db:migrate"
alias m="migrate"
alias rk="rake"
alias s="rspec"

# project aliases
alias dotfiles="cd ~/.dotfiles && sublime ."

# NPB
alias sshnpb="ssh deploy@192.184.83.75"
alias npb="cd /Users/nicholaspball/Sites/npb/rails"
alias npbdeploy="git push github master && cap production deploy"

# SCAREY
alias scarey="cd /Users/nicholaspball/Sites/scarey.org/site"
alias sshscarey="ssh -p 2222 nick@gator1332.hostgator.com"

# LURAY
alias luray="cd /Users/nicholaspball/Sites/luraymusic/site"
alias sshluray="ssh -p 2222 luray@gator3036.hostgator.com"

# VANILLA SLAB
alias vslab="cd /Users/nicholaspball/Sites/github/vanilla-slab"

# SITE
alias sites="cd /Users/nicholaspball/Sites/"

# the air on earth
alias air="cd /Users/nicholaspball/Sites/theaironearth"


# LOUDR
alias loudrweb='cd ~/Loudr/loudr-web && workon loudrweb'
alias loudrwebserv='cd ~./start.py'
alias workoff='deactivate'

